import React, { ReactNode } from "react"
import {z} from "zod"


type SplitScreenProps = {
    sides: ReactNode[]; 
  };
  
export const SplitScreen: React.FC<SplitScreenProps> = ({sides})=> {
return (
<div>

</div>)
}